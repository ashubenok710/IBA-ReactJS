import React from 'react';

const header = ({ headerText })=> {
    return (
        <header className="header">
            <h1>{headerText}</h1>
        </header>     
    )
}

export default header;